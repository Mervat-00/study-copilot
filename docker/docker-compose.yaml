
services:
  mongodb:
    image: mongo:7-jammy
    container_name: mongodb

    ports:
      - "27017:27017"

    volumes:
      - ./mongodb:/data/db

    networks:
      - book_qa_network

    restart: always

networks:
  book_qa_network: